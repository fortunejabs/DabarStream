# Core (required for importer + server + tests)
# Versions pinned from the working .venv on 2026-09-21 (Python 3.12).
# To update: pip install --upgrade <package> then re-run pip freeze.
flask==3.1.3
flask-socketio==5.6.1
python-socketio==5.17.0
simple-websocket==1.1.0
pytest==9.1.1

# Local capture client (Windows streaming machine)
# NOTE: faster-whisper requires Python 3.10-3.13; confirm wheel availability
# before upgrading the Python runtime on the streaming PC.
numpy==2.5.3
sounddevice==0.5.6
pyaudio           # no universal wheel; keep unpinned so pip picks the right build
faster-whisper==1.2.1

# Optional: global hotkey language switching (1=eng 2=bem 3=nya 4=ton).
# client.py degrades gracefully and disables hotkeys if this is absent.
pynput
